import { createContext } from "react";

/*
@dev VoteContext is a React Context that will be used to share the state of the vote between components
*/
const VoteContext = createContext();

export default VoteContext;
